 name: Greetings Pipeline

on: 
  workflow_dispatch:

# pipeline level pe agent ko assign ni kar sakte

jobs:
  greeting:
    name: Greeting Audience
    runs-on: windows-latest
    steps:
      - name: Hello Audience
        run: write-host "Hello Guys Whats's up"
